<nav class="navbar navbar-top navbar-expand-md navbar-dark" id="navbar-main">
    <div class="container-fluid">
        <!-- Brand -->
        <a class="h4 mb-0 text-white text-uppercase d-none d-lg-inline-block" href="{{url('admin_default')}}">Dashboard</a>
        <!-- User -->
        <ul class="navbar-nav align-items-center d-none d-md-flex">
            {# <li class="nav-item"> 
                <button class="btn btn-secondary nav-link position-relative" data-toggle="modal" data-target="#notificationModal">
                {% if app.user.getNotifications|length > 0 %}
                 <span class="position-absolute d-flex justify-content-center align-items-center text-center rounded-circle bg-danger text-white" style="width: 25px; height:25px;right:-10px; top:-10px">{{app.user.getNotifications|length}}</span>
                {% endif %}
                    <span class="nav-link-icon d-block text-primary"><i class="ni ni-bell-55"></i></span>
                </button>
            </li> #}
            <li class="nav-item dropdown">
                <a class="nav-link pr-0" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <div class="media align-items-center">
                        <span class="avatar avatar-sm rounded-circle">
                            <img alt="Image placeholder" src="{{ asset('argon') }}/img/theme/team-4-800x800.jpg">
                        </span>
                        <div class="media-body ml-2 d-none d-lg-block">
                            <span class="mb-0 text-sm  font-weight-bold">{{ app.user.username }}</span>
                        </div>
                    </div>
                </a>
                <div class="dropdown-menu dropdown-menu-arrow dropdown-menu-right">
                    <div class=" dropdown-header noti-title">
                        <h6 class="text-overflow m-0">Welcome!</h6>
                    </div>
                    {# <a href="{{ route('profile.edit') }}" class="dropdown-item">
                        <i class="ni ni-single-02"></i>
                        <span>{{ __('My profile') }}</span>
                    </a> #}
                    <div class="dropdown-divider"></div>
                    <a href="{{ url('app_logout') }}" class="dropdown-item">
                        <i class="ni ni-user-run"></i>
                        <span>Logout</span>
                    </a>
                </div>
            </li>
        </ul>
    </div>
</nav>