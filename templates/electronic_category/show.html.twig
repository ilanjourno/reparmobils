{% extends 'admin/base.html.twig' %}

{% set title = electronic_category.name %}
{% block header %}
    <a href="{{ path('electronic_category_index') }}" class="btn btn-primary">Retour</a>
{% endblock %}
{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th># :</th>
                <td>{{ electronic_category.id }}</td>
            </tr>
            <tr>
                <th>Nom :</th>
                <td>{{ electronic_category.name }}</td>
            </tr>
            <tr>
                <th>Créer le :</th>
                <td>{{ electronic_category.createdAt ? electronic_category.createdAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Modifier le :</th>
                <td>{{ electronic_category.updatedAt ? electronic_category.updatedAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="col mt-4">
        <div class="row">
            <a href="{{ path('electronic_category_edit', {'id': electronic_category.id}) }}" class="btn btn-primary">Modifier</a>

            {{ include('electronic_category/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
