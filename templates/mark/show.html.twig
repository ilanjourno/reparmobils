{% extends 'admin/base.html.twig' %}

{% block title %}Mark{% endblock %}
{% set title = mark.name %}
{% block header %}
    <a href="{{ path('mark_index') }}" class="btn btn-primary">Retour</a>
{% endblock %}
{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mark.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mark.name }}</td>
            </tr>
            <tr>
                <th>Créer le :</th>
                <td>{{ mark.createdAt ? mark.createdAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Modifier le :</th>
                <td>{{ mark.updatedAt ? mark.updatedAt|date('d-m-Y H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="col mt-4">
        <div class="row">
            <a href="{{ path('mark_edit', {'id': mark.id}) }}" class="btn btn-primary">Modifier</a>

            {{ include('mark/_delete_form.html.twig') }}
        </div>
    </div>
    
{% endblock %}
