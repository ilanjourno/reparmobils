{% extends 'admin/base.html.twig' %}

{% set title = "Ajouter un choix" %}
{% set type = param.type == 'colors' ?  'color' : 'text' %}
{% set label = param.type == 'colors' ? 'La couleur : ' ~ '<span class="text-danger">*</span>' : 'Pseudo :'%}

{% block header %}
    <a href="{{ path('electronic_params_values_index', {electronic: electronic_id, param: param.id}) }}" class="btn btn-primary">Retour</a>
{% endblock %}
{% block body %}

    {{ include('electronic_params_values/_form.html.twig') }}

{% endblock %}
